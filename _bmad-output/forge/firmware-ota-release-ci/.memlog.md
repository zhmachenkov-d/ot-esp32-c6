---
idea: OTA updates + CI/CD build on GitHub Releases
goal: Clarify what done means for OTA/release automation on ot-esp32-c6
status: complete
updated: 2026-09-09T06:47
---

- (direction) Clarify done: OTA/release automation on existing ot-esp32-c6
- (assumption) Device dual-slot OTA + HA update entity already exist; README says CI upload may be manual for v1; no .github/workflows found
- (note) User chose clarify (1); confirmed change to existing project
- (lock) In scope: CI/release automation only. Out of scope: device OTA feature changes; keep existing dual-slot/HA/manifest contract.
- (lock) Trigger A: push tag v* → CI builds → creates/updates GitHub Release and uploads assets
- (lock) Version policy: tag wins — CI injects tag version into the build so binary + manifest match vX.Y.Z
- (assumption) APP_FW_VERSION in source becomes a default/dev stub for local builds; release truth is the git tag
- (crack) Local USB flashes of main without a tag may report a version that never matches any Release; need to accept or document that
- (lock) Publish safety: create draft Release → upload .bin + manifest.json → publish only when assets complete
- (lock) Only strict vX.Y.Z tags run release workflow and publish as normal Release (/latest). No prerelease channel; other tags ignored or fail intentionally.
- (kill) Rejected: prerelease/suffixed-tag OTA channel for this round
- (lock) Release workflow: build (+ size gate) → draft → assets → publish. Host tests are not a tag/release gate.
- (assumption) Host tests may run on PR/main separately; not assumed to exist yet as part of this lock
- (lock) Done is wide: (1) tag vX.Y.Z release pipeline and (2) PR/main host-tests workflow so the split gate is real
- (lock) Host tests workflow: every PR into main + every push to main; path filters not required for done
- (lock) Release assets: otc6_gateway.bin + CI-generated manifest.json with version, url, sha256, size, release_url always set; no hand-edited Release JSON
- (crack) Surviving: tag releases are not gated by host tests; bad main can still be tagged and published. Local non-tag builds may report APP_FW_VERSION that never matches a Release.
- (direction) Session hardened; forged-idea.md and forge-report.html written
