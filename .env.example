# Dev secrets schema — copy to .env before the first dev container rebuild:
#   cp .env.example .env
#
# The .env file must exist; values are optional. Rebuild the dev container after
# changing .env (Dev Containers: Rebuild Container). Git push uses SSH; GH_TOKEN
# is for the gh CLI only (repo scope sufficient for PRs and API calls).
GH_TOKEN=
